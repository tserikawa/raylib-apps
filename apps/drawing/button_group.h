#ifndef UI_BUTTON_GROUP
#define UI_BUTTON_GROUP

#include "button.h"

typedef struct
{
    Button addButton;
    Button deleteButton;
    Button clearButton;
    Button selectButton;
    Button resetButton;
} ButtonGroup;

#endif